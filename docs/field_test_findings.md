# 実企業フォーム 適応テスト — 課題と改善案

**実施日**: 2026-07-06
**目的**: 構造の異なる実企業の問い合わせフォームに、L1発見〜L4入力（Planフェーズ）がどこまで適応できるかの検証
**重要**: 本テストで**送信は一切していない**。全てPlanフェーズ（入力＋確認画面/入力済みフォームのスクショ取得）まで。

---

## 1. テスト条件

- 対象: 実在企業 **22社**（介護9・建設6・物流7、＝WebSearchで収集した業界横断リスト [scripts/test20_companies.csv](../scripts/test20_companies.csv)）
- フロー: `ingest`(L0) → `discover`(L1発見+L2解析) → `plan`(L4 Plan=入力+スクショ)
- 送信者情報: ネオキャリア株式会社 / 福井 翔 / sho.fukui@neo-career.co.jp / 電話は仮値 `03-1234-5678`

## 2. 結果サマリー

| 区分 | 件数 | 備考 |
|---|---|---|
| ✅ 入力＋スクショ完了（承認待ち PENDING_APPROVAL） | **11** | 発見→解析→入力まで自動化成功 |
| ⛔ フォーム未発見（FORM_NOT_FOUND） | 10 | SPA/iframe埋込・メールのみ等 |
| 🚫 抑制（SUPPRESSED） | 1 | 「営業メールなどはご遠慮」を自動検知しコンプラ除外 |

発見できた11社のゲート内訳: **mid（良好）3 / low 5 / block（CAPTCHAで手動）3**

発見率（フォーム到達）: 12/22 ≒ **55%**（大手のiframe/SPAフォームを含む最悪ケース混在での値。実ICPのSMBはより高い想定）

### 個社の主な観測

| # | 企業 | 業界 | gate | 観測 |
|---|---|---|---|---|
| 1 | ケア21 | 介護 | block | /contact/ 発見、確認画面あり、**reCAPTCHA v3 検知** |
| 2 | 介護のミモザ | 介護 | mid | 氏名/メール/電話/本文は入力成功。ただし**介護相談フォーム**（消費者向け）で必須項目多数（フリガナ/住所/介護状況ラジオ/メール確認/同意）が未入力 |
| 7 | 明昭 | 介護 | block | reCAPTCHA v3 |
| 18 | ダイセル物流 | 物流 | mid | **会社名「ネオキャリア株式会社」・メール・本文・個人情報同意チェックが正しく入力**。残エラーは フリガナ・郵便番号(分割)・電話(3分割)・種別select |
| 21 | 岡山県貨物運送 | 物流 | block | reCAPTCHA v3 |
| 5 | フロンティアの介護 | 介護 | (抑制) | **「営業メールなどはご遠慮」検知 → SUPPRESSED**（コンプラ§9が実サイトで機能） |

---

## 3. 効いている点（構造が違っても共通して機能）

- **主要テキスト項目の入力**: 会社名・氏名・メール・問い合わせ本文（署名付き）・個人情報同意チェックを、全く異なる構造の実フォームで正しく入力
- **ハニーポット検知・非入力**: honeypot判定した隠しフィールドを埋めない（company #9で実際に email ロールのhoneypot欄をスキップ）
- **reCAPTCHA v2/v3 検知**: 検知したら gate=block ＝ 手動送信キューへ回す
- **確認画面ハンドリング**: 「確認画面へ」ボタンを分類してクリックし、確認画面/入力済みフォームをスクショ
- **コンプラ抑制**: 「営業お断り」系文言を検知して自動除外
- **送信の非実行**: Planフェーズで最終送信を一切しない設計が実サイトでも守られた

---

## 4. 発見した課題（改善が必要）

スクショ（[artifacts/plan_*.png](../artifacts/)）で露見した、複雑フォームで未入力が残る要因。

### 課題A（最優先）— 分割フィールド未対応
1つの論理項目が複数の `<input>` に分かれているケースを、現状は先頭のボックスに全部入れてしまう。
- **電話番号** 3分割（市外/市内/番号）: 例 ダイセル物流で「03-1」だけ入り検証エラー
- **郵便番号** 2分割
- **氏名** 姓/名 分割: 「福井 翔」を姓欄に丸ごと投入
- **住所** 都道府県(select)＋市区町村(text) 分割

**影響度**: 大。日本のフォームの多くが電話・氏名・住所を分割。これが未対応だと mid/low に留まる主因。

### 課題B — フリガナ（kana）未生成
氏名のフリガナが必須の場合でも生成していない（送信者名「福井 翔」が漢字のため空のまま）。→ 必須バリデーションで弾かれる。

### 課題C — 必須の select / radio 未選択
- お問い合わせ種別・ご希望の商談種別などの **必須プルダウン** を自動選択していない
- 介護状況・認知症などの **必須ラジオ**（※これは消費者向けフォームで、そもそも対象外の可能性）

### 課題D — メール確認欄（confirm email）未対応
「メールアドレス（確認）」の再入力欄にロールが割り当たらず空になる。

### 課題E — フォームの「種類」の見分け
介護相談・資料請求(brochure)など、**B2B営業窓口でない消費者向けフォーム**を発見してしまうことがある（例 #2 ミモザ＝介護相談、#9 みんかい＝/brochure 資料請求, conf=0.223）。営業文面と項目がミスマッチ。

### 課題F — 発見率（大手のiframe/SPA）
戸田建設・前田建設・JR貨物・ツクイ 等は静的にもブラウザレンダリングでもフォーム未検出。**クロスオリジンiframe埋込**（HubSpot/Marketo等）や独自SPAが原因。

---

## 5. 改善案（優先度順）

| 優先 | 課題 | 改善案 | 想定効果 |
|---|---|---|---|
| **P1** | A 分割フィールド | L2で「同一ラベル配下の連続input群」を検出し、電話/郵便/氏名/住所を**サブフィールド分割ルール**で個別マッピング。値も分割して投入（電話→ハイフン分割、氏名→姓/名） | mid/lowの多くがhighへ。未入力の最大要因を解消 |
| **P1** | B フリガナ | 送信者情報に `SENDER_KANA_SEI/MEI` を追加し、kanaロールに投入。将来はLLMで漢字→カナ推定も可 | フリガナ必須フォームが通る |
| **P2** | C 必須select/radio | 必須の `<select>` は「選択してください」以外の先頭値 or ラベル一致で自動選択。ラジオは「その他/法人/営業」系を優先選択。**曖昧なものはgateを下げて人間へ** | 確認画面到達率↑ |
| **P2** | D メール確認欄 | ラベルに「確認/再入力/confirm」を含むメール型は `email_confirm` ロールとして email と同値を投入 | 確認欄ありフォームが通る |
| **P2** | E フォーム種別判定 | ページ/フォーム文言から「相談・資料請求・予約・採用応募」等の**非営業窓口を分類**し、gate=lowまたは除外候補に。真のB2B問い合わせ窓口を優先 | 誤フォーム送信の防止（ブランド保護） |
| **P3** | F iframe/SPA発見 | 同一オリジンiframe内フォームの検出・入力対応。クロスオリジン(HubSpot等)は埋込元URLを辿るか手動キューへ | 大手の発見率↑（ただし実ICPでは優先度低） |

### 実装順の推奨
1. **課題A（分割フィールド）** から着手 ── 費用対効果が最大。L2のマッピングとL4の値投入の両方に手を入れる
2. 次に **B（フリガナ）＋D（メール確認）** ── 設定追加＋ロール追加で軽量
3. その後 **C（必須select/radio）** と **E（フォーム種別判定）** ── 品質・ブランド保護の底上げ
4. **F（iframe/SPA）** は P2フェーズで（実ICPのSMBで発見率を実測してから判断）

---

## 6.5 改善実装ログ

### 2026-07-09 — 課題A（分割フィールド）＋B（フリガナ）＋D（メール確認）を実装・検証

分割フィールドを「サブロール」基盤としてまとめて解消。フォーム埋め精度の最大要因を除去した。

- **新レイヤ [src/layers/l2_split.ts](../src/layers/l2_split.ts)**: 論理1項目が複数inputに分かれるケースを2系統で検出
  - 属性規約: `tel1/tel2/tel3`・`sei/mei`・`lastname/firstname`・`zip1/zip2`・autocomplete(`tel-area-code`等)・メール確認欄(`確認/再入力/2`)
  - 構造的隣接: 同一ラベル（例「電話番号」）配下の連続input群を位置で分割
- **サブロール追加** ([types.ts](../src/types.ts)): `name_sei/name_mei`・`kana_sei/kana_mei`・`phone1/2/3`・`postal1/2`・`email_confirm`。`SPLIT_TO_BASE` で基底ロールへ畳み込み
- **値生成** ([l3_content.ts](../src/layers/l3_content.ts)): 電話をハイフン分割、氏名を姓/名分割、フリガナは `SENDER_KANA_SEI/MEI` から、郵便を上3桁/下4桁分割、メール確認は同値。**真実の送信者情報のみ使用し、未設定値は捏造せず空**（→必須欠落はゲートで人手へ）
- **投入** ([l4_submit.ts](../src/layers/l4_submit.ts)): サブロールを `TEXT_ROLES` に追加
- **ゲート** ([gate.ts](../src/core/gate.ts)): 分割された `name` を「両半分が揃えば充足」として必須判定に反映
- **設定** ([config.ts](../src/config.ts) / .env): `SENDER_KANA_SEI`・`SENDER_KANA_MEI`・`SENDER_POSTAL`・`SENDER_DEPARTMENT`

**検証**: unit 12件 PASS（分割検出・非分割の誤検出なし・値分割）。E2E に分割フォーム `/split`（姓名・セイメイ・郵便2分割・電話3分割・メール確認）を追加し、**全14項目を個別入力→サーバ側で各パート必須＋メール一致を検証→送信成功**。従来 mid/low 相当のフォームが **gate=high** に到達。

**残課題**: C（必須select/radio）・E（フォーム種別判定）・F（iframe/SPA発見）。次反復で C→E の順に着手予定。

### 2026-07-09 — 課題C（必須select/radio 自動選択）を実装・検証

必須の `<select>`（お問い合わせ種別 等）と必須ラジオ（法人/個人 等）を、ブランド安全な中立値で自動選択。未選択のまま必須バリデーションで弾かれる問題を解消。

- **新レイヤ [src/layers/l2_choice.ts](../src/layers/l2_choice.ts)** + ロール `choice`（[types.ts](../src/types.ts)）
  - `<select>`: プレースホルダ（「選択してください」等）を除外し、優先キーワード（`法人/企業/その他/サービス/製品/…`）一致 → なければ先頭の実選択肢。フォールバック選択は `ambiguous` フラグ
  - radioグループ: **キーワード一致時のみ選択**。非一致の必須ラジオ（介護状況・性別等の恐れ）は**推測せず人手へ**回す（ブランド保護）
- **値の運搬**: parse時に決めた選択肢を `FieldMapping.value` に載せ、[l4_submit.ts](../src/layers/l4_submit.ts) が select=`selectOption` / radio=`check` で投入
- **ゲート** ([gate.ts](../src/core/gate.ts)): `ambiguousChoice` の時は `high`→`mid` に下げ、必ず人間レビューを挟む（自動送信を防ぐ）

**検証**: unit 20件 PASS（キーワード優先・フォールバック・非該当ラジオを推測しない・任意フィールド不介入・既マップskip）。E2E の `/split` に必須select（種別）＋必須radio（区分）を追加し、**その他/法人 を自動選択→サーバ側で両必須を検証→送信成功**、確信選択のため **gate=high 維持**。

**残課題**: E（非営業窓口フォームの分類でブランド保護）・F（iframe/SPA発見）。次反復で E に着手予定。

## 6. 再現方法

```bash
cd toiawase_form_sales_auto_system
npm run cli -- ingest scripts/test20_companies.csv
npm run cli -- discover --limit 30      # L1+L2（実ブラウザ、数分）
npm run cli -- plan --limit 30          # L4 Plan＝入力+スクショ（送信なし）
npm run serve                            # http://localhost:4599 で人間レビュー
```

スクショは `artifacts/plan_<companyId>.png`。承認ダッシュボードで 承認/却下/抑制 を操作。
